# **产品设计文档：DeepList (无限嵌套清单 App)**

版本: 2.2 (Provider + Signals)  
日期: 2025-12-11

## **1. 产品概述 (Overview)**

### **1.1 设计哲学**

"Structure at the speed of thought."
DeepList 旨在提供一种无摩擦的层级化思考体验。它不仅仅是一个待办清单，更是一个外部大脑，允许用户通过无限嵌套的节点来拆解复杂任务。

### **1.2 核心差异化**

* **原生触感 (Native Feel)**: 严格遵循 Material 3 设计规范，提供仿佛系统自带应用般的流畅度。
* **极致性能 (High Performance)**: 基于 **Isar Database**，轻松支撑数万节点的毫秒级查询与渲染。
* **树状结构 (Tree Structure)**: 应用程序维护一个固定的树状节点结构，只有一个根节点，其 ID 固定不变，标题为 "DeepList"。

### **1.3 应用结构**

* **固定根节点**: 应用程序的根节点 ID 固定不变，标题为 "DeepList"，作为所有内容的顶级容器。
* **主页面**: 主页面显示根节点的视图，即根节点的子节点列表。
* **导航**: 用户可以聚焦到任何节点，使其成为新页面的 Root，形成无限嵌套的导航体验。


## **2. 交互与用户体验设计 (UX Design)**

### **2.1 导航架构 (Navigation)**

采用 **沉浸式聚焦导航 (Immersive Focus Navigation)**：

* **聚焦模式 (Zoom In)**: 点击列表项右侧的 Chevron Right 图标，页面 Push 进栈，当前节点变为新页面的 Root。  
* **返回**: 使用系统原生的左侧边缘右滑手势 (Swipe Back) 或顶部 AppBar 的返回按钮。


### **2.2 列表项交互 (Material List Item)**

#### **A. 视觉结构 (Visual Structure)**

基于 Material 3 ListTile 进行定制：

* **Leading**: Checkbox (M3 样式，圆形/圆角矩形)。  
* **Body**:  
  * TextField (Title): 无边框，主要字体 (BodyLarge)。  
  * TextField (Note): 仅在非空或获得焦点时展开，次要字体 (BodyMedium, OnSurfaceVariant)。  
  * **Status Indicator**: 若任务被收藏，标题旁显示微型 Star 图标 (颜色: Tertiary)。  
* **Trailing**:
  * 动态指示器: 若有子节点，显示微型进度环 (Progress Ring) 或 子节点计数 (Badge)。
  * 跳转按钮: IconButton (icon: arrow_forward_ios, size: small)。

#### **B. 选择与编辑状态 (Selection and Edit State)**

1. **选定状态触发**: 点击列表项的 **Checkbox** 或 **Leading 区域** 可将其设置为选定状态。
2. **选定状态视觉**: 选定状态的列表项边框高亮显示（使用Primary颜色），背景颜色可轻微变化以突出显示。
3. **编辑权限**: 只有在选定状态下的列表项，点击其标题或备注区域才会进入编辑模式，允许用户输入或修改内容。
4. **多选模式**: 在选定一个项目后，可以继续选择其他项目以进行批量操作。

#### **C. 手势操作 (Gestures - Powered by flutter_slidable)**

| 手势方向 | 动作 | 颜色语义 (M3 ColorScheme) | 逻辑定义 |
| :---- | :---- | :---- | :---- |
| **右滑 (Start -> End)** | **删除+剪切+归档** | Secondary (次色) | 弹出图标选择菜单：删除（垃圾桶图标）、剪切（剪刀图标）、归档（归档图标）。 |
| **左滑 (End -> Start)** | **切换状态** | Primary (主色) | 切换节点的完成状态 (Done/Undone) 或 任务类型 (如 Todo <-> Header)。（图标：勾选）|
| **左滑 (End -> Start)** | **收藏** | Tertiary (第三色) / Gold | 标记/取消标记为"重点关注"，在列表中增加星标高亮。（图标：星标）|

**优化点**:

* **阻尼感**: 滑动操作应配合 HapticFeedback.lightImpact() 提供触觉反馈。
* **图标显示**: 所有滑动操作只显示图标，不显示文字标签。
* **归档反馈**: 触发归档后，节点移至归档列表且在当前视图中隐藏。

#### **C. 输入流 (Input Flow) - 核心体验**

为了实现"心流"，必须处理好 FocusNode 的传递。

1. **回车 (Enter / Submit)**:
   * **光标在末尾**: 在下方创建新兄弟节点 -> 焦点跳转至新节点。
   * **光标在中间 (Split)**:
     * 原节点保留光标前文本。
     * 新节点（下方兄弟）获取光标后文本。
     * 焦点跳转至新节点开头。
2. **退格 (Backspace)**:
   * **光标在开头 & 文本为空**: 删除当前节点 -> 焦点跳回上一节点末尾。
   * **光标在开头 & 文本不为空 (Merge)**:
     * 将当前文本追加到上一节点 Note 或 Title 末尾。
     * 删除当前节点。
     * 移动子节点（如有）到上一节点下。
     * 焦点跟随光标位置。

#### **D. 拖动与排序交互 (Drag and Sort Interaction)**

1. **触发拖动排序**: 长按列表项可触发拖动排序模式。
2. **拖动体验**: 长按时项目会轻微抬起（增加Elevation）并显示拖动手柄，用户可上下拖动项目到新位置。
3. **排序逻辑**: 在拖动过程中，其他项目会动态调整位置为被拖动项目让出空间。
4. **持久化**: 拖动结束后，通过业务逻辑层的updateNode方法更新父节点的children列表顺序。

## **3. 技术架构 (Technical Architecture)**

### **3.1 核心技术栈**

* **Framework**: Flutter (Dart 3.x)  
* **Database**: **Isar** (v4.0+) - 极速、ACID 事务、全文本搜索支持。  
* **Dependency Injection**: **Provider**。用于注入 Isar 实例、Repositories 和 Services。  
* **State Management**: **Signals** (signals_flutter)。  
  * 使用 Signal<T> 管理可变状态。  
  * 使用 StreamSignal 直接绑定 Isar 的 Watch Stream，实现零样板代码的实时 UI 更新。  
  * 优势：细粒度更新（只重绘变化的 Widget），无需重构整个 Widget Tree。  
* **Theme**: Material 3 (使用 ThemeData(useMaterial3: true, colorSchemeSeed: ...)).

### **3.2 数据模型 (Isar Schema)**


// 伪代码预览，详见 todo_schema.dart
const Id kRootNodeId = 1; // 固定根节点 ID，禁止删除/移动

@collection
class NodeItem {
  // 根节点使用 kRootNodeId，其余使用 Isar.autoIncrement
  Id id;

  Id? parentId; // 父节点 ID，根节点为 null

  String content = '';
  String? note;

  bool isDone = false;
  bool isFavorite = false; // 新增：收藏状态
  bool isArchived = false; // 新增：归档状态
  List<Id> children = []; // 初始化以确保生成/排序正常

  DateTime createdAt;
  DateTime updatedAt;
}

// 应用程序有且仅有一个根节点，其 ID 固定不变，标题为 "DeepList"。
// 主页面显示根节点的视图，即根节点的子节点列表。
// 初始化数据库时：若不存在根节点，则插入 NodeItem(id: kRootNodeId, parentId: null, content: "DeepList", children: [], createdAt: now, updatedAt: now)。
// 写入规则：插入/移动/删除必须同时维护 parentId 与父节点 children 顺序（先更新子节点的 parentId，再更新父节点的 children，使用事务保证一致）。
// 时间字段：创建时同时赋值 createdAt、updatedAt；更新时仅刷新 updatedAt，不依赖 Isar 默认值。

### **3.3 业务逻辑层 (Repository Layer)**

需封装 NodeRepository 类处理 Isar 操作，UI 层通过 Signals 消费这些数据（统一使用 NodeItem/Id 类型）：

* Id getRootNodeId(): 获取固定根节点的 ID（kRootNodeId），标题为 "DeepList"，ID 固定不变。
* Stream<List<NodeItem>> watchChildren(Id? parentId): 实时监听某层级下的数据变化（默认过滤掉 isArchived 为 true 的节点）。在 ViewModel 中可转换为 StreamSignal。
* Stream<NodeItem?> watchById(Id nodeId): 观测单一节点的数据变化。在 ViewModel 中可转换为 StreamSignal。
* Future<void> updateNode(NodeItem item): 更新节点信息。
* Future<void> removeNode(Id nodeId): 从数据库中移除节点。
* Future<Id> insertNode(NodeItem item, {Id? parentId}): 将节点插入到指定父节点下，返回新建节点 ID（根节点除外）。

## **4. UI 视觉规范 (Material 3 Implementation)**

### **4.1 配色系统 (Color System)**

放弃硬编码颜色，使用主题颜色。

* **Seed Color**: #0061A4 (Deep Blue)  
* **Light Theme**:  
  * Background: Surface / SurfaceContainerLow (#F7F9FC)  
  * Card/Tile: SurfaceContainerLowest (#FFFFFF)  
  * Text: OnSurface  
  * Checkbox (Checked): Primary  
* **Dark Theme**:  
  * 自动反转，背景为深灰/黑 (#1A1C1E)，文字为白色。

### **4.2 字体排印 (Typography)**

使用 Flutter 的 TextTheme：

* **List Title**: BodyMedium   -> 选中后增加删除线，颜色变为 OnSurfaceVariant。  
* **List Note**: BodySmall  -> 颜色 Outline。  
* **AppBar Title**: TitleMedium  

### **4.3 布局 (Layout)**

* **Indent Guide**: 在子节点列表左侧绘制细线条（VerticalDivider），颜色使用 OutlineVariant，帮助用户识别层级深度。  
* **Elevation**: 拖拽排序时，被拖拽的 Item 增加 Elevation (Level 3) 和阴影，增强物理质感。

## **5. 核心逻辑算法优化 (Logic Specifications)**

### **5.1 剪切逻辑 (Cut Logic)**

用户向右滑动节点 A。

1. **Cut Action**: 用户点击剪切，**数据库不进行任何修改**。
2. **Visual Highlighting**: 剪切后的节点在原位置进行视觉突出显示（例如半透明或虚线边框），表示它处于“待移动”状态。
3. **Global State**: 将该节点 ID 存储在全局剪切状态 (Clipboard Signal) 中。
4. **UI Feedback**: 底部工具栏显示"粘贴"与"取消"按钮，不再显示SnackBar。
5. **Paste Operation**: 当点击底部"粘贴"按钮时：
   * **循环引用校验**: 检查目标父节点是否为“被移动节点”本身或其任何后代节点。若是，则阻止操作并提示错误。
   * 执行数据库事务：将节点从原父节点的 `children` 列表中移除 -> 添加到当前父节点的 `children` 列表中 -> 更新该节点的 `parentId`。
   * 清空全局剪切状态，移除视觉高亮。
6. **Cancel Operation**: 当点击底部"取消"按钮时，移除节点的视觉突出显示并将全局的剪切状态设为空。

### **5.2 收藏逻辑 (Favorite Logic)**

用户向左滑动节点 B 并点击"收藏"。

1. **Toggle**: 读取 isFavorite 字段并取反。
2. **Persist**: item.isFavorite = !item.isFavorite; item.save();
3. **Visual Update**:
   * 若为 true: 标题旁显示实心星标（具体图标与颜色方案待定），或背景色轻微泛黄（使用 SurfaceContainerHigh）。
   * 若为 false: 恢复默认样式。
4. **Sort Effect (Optional)**: 可选逻辑——收藏的节点在当前层级中置顶，或者仅作为视觉标记。当前版本默认为**仅视觉标记**。
   * **待完善**: 星标的具体图标与颜色方案需在 UI 实现阶段明确。

### **5.3 排序逻辑 (Sort Logic)**

用户长按列表项并拖动以重新排序。

1. **Drag Detection**: 检测长按动作并进入拖动模式，被拖动项视觉抬起（增加Elevation）。
2. **Visual Feedback**: 拖动过程中，其他项动态调整位置，为被拖动项让出空间。
3. **Order Update**: 拖动结束后，更新父节点的子节点列表中ID的顺序，不需要修改节点本身的任何属性。
4. **Persistence**: 调用 updateNode 方法更新父节点的 children 列表顺序，保存排序变更。

### **5.4 归档逻辑 (Archive Logic)**

用户通过右滑弹出的图标选择菜单点击"归档"图标。

1. **Toggle Archive Status**: 设置节点的 isArchived 属性为 true。
2. **Update Database**: 调用 updateNode 方法更新节点的归档状态。
3. **Visual Update**:
   * 节点在当前视图中隐藏（通过过滤条件不在列表中显示）。
   * 节点仍保留在原始父节点下，但标记为已归档状态。
4. **Archive View**: 已归档的节点可通过专门的归档视图进行访问和管理。
   * **待完善**: 尚未定义“取消归档/恢复”流程与 UI；watchChildren 的默认过滤规则需确认（当前假设过滤掉 isArchived 为 true）。

## **6. 开发路线修正 (Revised Roadmap)**

### **Phase 1: 数据驱动骨架**

* [ ] 配置 Isar 生成 Schema (包含 isFavorite 字段)。  
* [ ] 配置 Provider 进行依赖注入。  
* [ ] 实现 NodeRepository (CRUD)。  
* [ ] 编写单元测试验证“数组排序”和“递归删除”逻辑。  
  * **待完善**: “递归删除”与“数组排序/分数索引”算法细节未在正文给出，测试方案需后续补充具体逻辑描述。

### **Phase 2: M3 UI 与基础交互**

* [ ] 使用 Watch (Signals) + StreamBuilder/ListView 实现沉浸式聚焦导航列表（单层视图）。  
* [ ] 集成 flutter_slidable 并配置自定义 ActionPane (Cut/Favorite/Toggle)。

### **Phase 3: 心流体验 (The Flow)**

* [ ] 实现 FocusManager，处理回车/退格的焦点跳转。  
* [ ] 增加震动反馈与过渡动画 (AnimatedList)。
