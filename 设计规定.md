# DeepList 架构与实施约定

本项目当前按“从零重建”的基础架构维护，不兼容历史持久化、状态管理和数据模型，也不迁移开发阶段旧数据库。

## 目标架构

- Flutter + Material 3。
- Riverpod 是唯一的应用级状态管理方案；页面和组件内部只保留真正的 Widget 生命周期状态。
- Drift + SQLite 是持久化层，go_router 是导航唯一真相。
- Domain 不依赖 Flutter、Riverpod 或 Drift。
- 依赖方向为 Presentation → Application → Domain，Data 实现 Domain 接口。

主要目录如下：

```text
lib/
├── app/                         # bootstrap、router、Riverpod providers、App 壳
├── core/
│   ├── database/                # Drift schema 与 AppDatabase
│   └── theme/                   # Material 3 light/dark/system
└── features/nodes/
    ├── domain/                  # Node、NodeId、Repository 契约、树规则
    ├── application/             # TreeCommandService、页面状态、剪贴板
    ├── data/                    # DriftNodeRepository
    └── presentation/            # NodePage、NodeList、EditorSession、NodeCard
```

## Node 与树关系

Node 的唯一层级关系是 `parentId`：

- `parentId == null` 表示顶级节点，不创建实体根节点。
- `id` 是 UUID 字符串，不使用数据库自增整数。
- sibling 顺序由 `position` 表示，第一版在事务内连续重编号为 `0, 1, 2 ...`。
- 节点只保存 `isArchived`，数据库中不保存反向子列表数组。
- `nodes(parent_id, position)` 和 `nodes(parent_id, is_archived, position)` 是查询索引。
- `parent_id` 是 self-referencing foreign key，并由数据库 CHECK 拒绝直接 self-parent。

`TreeCommandService` 是全部树结构写操作的唯一入口。`createNode`、`moveNode`、`reorderChildren`、`deleteSubtree`、`archiveNode`、`restoreNode`、split、merge、indent 和 outdent 都通过 Repository transaction 完成。移动前会验证目标存在、self-parent 和祖先链；祖先遍历包含 visited 集合，因此旧坏数据中的 cycle 不会造成死循环。

Archive 只切换 `isArchived`，不改变 parent-child 关系。Delete 是递归删除整个 subtree，并在同一事务内修复原 sibling 的 position。任何事务异常都会 rollback。

## 状态和编辑生命周期

- SQLite 保存持久化 Node；Drift Repository 将数据库行转换为 Domain Node。
- `childrenProvider(parentId)` 和 `nodeProvider(nodeId)` 是数据库查询的 Riverpod 反应式边界；首页直接使用 `childrenProvider(null)`。
- `NodePageController(parentId)` 只保存页面交互状态，例如当前编辑节点和选择集合，不缓存 Node 列表。
- `NodeCard` 是列表单节点组件。每个编辑中的 Card 自己拥有并释放 `TextEditingController` 和 `FocusNode`。
- `EditorSession` 只负责 Presentation 层的注册、显式 focus、cursor、commit、unfocus 和 end editing，不包含树业务逻辑。
- 禁止依赖 TextField 的自动聚焦。Enter、Backspace、新建和导航都通过 `EditorSession.focus` 显式请求焦点。
- push 或 pop 前先 commit 当前输入并结束编辑；页面销毁时释放 EditorSession。返回旧页面不会自动恢复 TextField focus 或键盘。

## 第一阶段用户流程

当前最小纵向流程是：启动 → 查看顶级节点 → 新增节点 → 编辑文本 → Enter split → 点击进入节点 → 新增 child → 返回。高级归档界面、拖拽排序界面、剪切粘贴 UI 和设置页可在基础架构稳定后继续实现；其底层命令边界已经预留。

## 验证要求

提交前至少运行：

```text
dart format lib test
dart run build_runner build --delete-conflicting-outputs
flutter analyze
flutter test
```

测试按 domain、application、data、presentation 分层，重点覆盖 self-parent、cycle、dangling parent、事务 rollback、递归删除、排序、归档关系和编辑器 focus 生命周期。
