# DeepList 架构与实施约定

本项目当前按“从零重建”的基础架构维护，不兼容历史持久化、状态管理和数据模型，也不迁移开发阶段旧数据库。

## 目标架构

- Flutter + Material 3。
- Riverpod 是唯一的应用级状态管理方案；页面和组件内部只保留真正的 Widget 生命周期状态。
- Drift + SQLite 是持久化层，go_router 是导航唯一真相。
- Domain 不依赖 Flutter、Riverpod 或 Drift。
- 依赖方向为 Presentation → Application → Domain，Data 实现 Domain 接口。

主要目录如下：

```text
lib/
├── app/                         # bootstrap、router、Riverpod providers、App 壳
├── core/
│   ├── database/                # Drift schema 与 AppDatabase
│   └── theme/                   # Material 3 light/dark/system
└── features/nodes/
    ├── domain/                  # Node、NodeId、Repository 契约、树规则
    ├── application/             # TreeCommandService、页面状态、剪贴板
    ├── data/                    # DriftNodeRepository
    └── presentation/            # NodePage、NodeList、EditorSession、NodeCard
```

## Node 与树关系

Node 的唯一层级关系是 `parentId`：

- `parentId == null` 表示顶级节点，不创建实体根节点。
- `id` 是 UUID 字符串，不使用数据库自增整数。
- sibling 顺序由 `position` 表示，第一版在事务内连续重编号为 `0, 1, 2 ...`。
- 节点只保存 `isArchived`，数据库中不保存反向子列表数组。
- `nodes(parent_id, position)` 和 `nodes(parent_id, is_archived, position)` 是查询索引。
- `parent_id` 是 self-referencing foreign key，并由数据库 CHECK 拒绝直接 self-parent。

`TreeCommandService` 是全部树结构写操作的唯一入口。`createNode`、`copySubtree`、`moveNode`、`reorderChildren`、`deleteSubtree`、`archiveNode`、`restoreNode`、split、merge、indent 和 outdent 都通过 Repository transaction 完成。移动前会验证目标存在、self-parent 和祖先链；祖先遍历包含 visited 集合，因此旧坏数据中的 cycle 不会造成死循环。

Archive 只切换 `isArchived`，不改变 parent-child 关系。Delete 是递归删除整个 subtree，并在同一事务内修复原 sibling 的 position。`copySubtree` 递归复制完整子树并为所有节点生成新 UUID，插入在目标节点下方同级。任何事务异常都会 rollback。

## 节点交互与编辑生命周期

交互核心原则：**点击编辑，箭头进入，长按管理，拖动排序，滑动改层级**。彻底摒弃 `Selected` 状态机。

1. **状态机规范**：
   - `PageMode` 仅包含 `normal`、`editing`、`dragging`。不存在任何 `Selected` 中间状态。
   - `NodePageState` 仅维护 `mode` 和 `activeNodeId`。
   - 返回逻辑：Editing 状态返回 Normal；Normal 状态返回上一级（Pop）。

2. **操作与入口解耦**：
   - **无拖动手柄与视觉固定**：节点无永久拖动手柄（移除 `Icons.drag_indicator`），左侧边距恢复固定（12dp），行高与宽度稳定无跳变。
   - **内容编辑（单击正文）**：单击未编辑节点直接进入编辑态（`PageMode.editing`）。处于编辑态时由 `TextField` 自身接管光标定位与文本选择，长按文字为系统划词选择。
   - **普通纵向拖动只滚动列表**：普通上下滑动由列表滚动物理接管，不触发任何拖动排序。
   - **页面导航（右侧 Trailing Slot）**：槽位固定 48dp 宽度。Chevron `›` 永久靠右存在。当 `childCount > 0` 时显示计数。点击 Chevron 永远只负责进入节点子页面，即使在编辑态也允许进入，绝不触发正文编辑。
   - **节点管理（长按后无明显移动松手 / 键盘 •••）**：非编辑态长按达到阈值（`kLongPressTimeout`）轻微震动，若松手前无明显移动（移动 ≤ 12dp）则在下一帧打开 `NodeActionMenu` 底部菜单（复制、粘贴、归档、删除），不改变 `PageMode`。编辑态下点击 `KeyboardToolbar` 的 `•••` 打开完全相同的菜单。
   - **同级排序（长按达到阈值后上下移动）**：由 `NodeReorderRegion` 包装，长按达到阈值后上下移动超过 12dp 视为同级重排。拖动仅调用 `TreeCommandService.reorderChildren`，严格限制在当前同级重排。**Drag never changes hierarchy**（拖动绝不改变 parentId、跨层级或跨页面）。
   - **层级调整（左右滑）**：**Hierarchy is controlled only by indent/outdent**。仅在非编辑态响应左右滑动（右滑 Indent，左滑 Outdent）。编辑态横向手势让渡给文本光标，由 `KeyboardToolbar` 的减少/增加层级按钮执行。
   - **Editing 时禁用节点级长按**：Editing 状态下 `NodeReorderRegion` 处于禁用状态（`enabled: false`），TextField 完全保留系统文字选择与划词长按行为，不触发节点菜单或拖动重排。
   - **焦点无缝交接 (Focus Handover)**：A 处于编辑态时点击 B 或点击尾部空白新建 B，A 的文本自动提交（若为空则删除），通过 `EditorSession.handoverFocus` 直接转移焦点到 B，软键盘全程保持展开，不退出编辑态。

3. **生命周期与焦点**：
   - `EditorSession` 负责 Presentation 层的注册、显式 focus、handoverFocus、commit、unfocus。
   - 禁止依赖 TextField 的自动聚焦。Enter、Backspace、新建和导航都通过 `EditorSession` 显式交接与聚焦。
   - push 或 pop 前先 commit 当前输入并结束编辑；页面销毁时释放 EditorSession。返回旧页面不会自动恢复 TextField focus 或键盘。

## 验证要求

提交前至少运行：

```text
dart format lib test
dart run build_runner build --delete-conflicting-outputs
flutter analyze
flutter test
```

测试按 domain、application、data、presentation 分层，重点覆盖 self-parent、cycle、dangling parent、事务 rollback、递归删除、排序、归档关系和编辑器 focus 生命周期。
